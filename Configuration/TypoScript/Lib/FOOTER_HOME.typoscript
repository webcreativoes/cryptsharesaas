lib.FOOTER_HOME = COA
lib.FOOTER_HOME {
    10 = CONTENT
    10 {
        table = tt_content
        select {
            orderBy = sorting
            where = colPos = 0
            pidInList.data = page:uid
            pidInList.override = {$PID_FooterRowHome}
        }
        wrap = <footer class="home-navbar d-none d-sm-block" id="footer">|</footer>
    }
}
